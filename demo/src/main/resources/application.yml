osf:
  server: mongodb
  timeToLive: 30000
  minio:
    endpoint: http://127.0.0.1:9000
    accessKey: minioadmin
    secretKey: minioadmin
    bucketName: test

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: test

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest